export interface School {
    name: String,
    description: String,
    date: String
}

export interface Skill {
    name: String,
    description: String,
    level: String
}  

export interface Experience {
    organisation: String,
    description: String,
    role: String,
    date: String
}